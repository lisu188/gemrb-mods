name: Active schema test patch

on:
  push:
    branches:
      - agent/review-mod-completeness
    paths:
      - tools/patch_active_schema_detection.py
      - .github/workflows/active-schema-test-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/review-mod-completeness
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Patch lifecycle schema detection
        run: python tools/patch_active_schema_detection.py
      - name: Validate patched sources
        run: |
          python cipher/tests/validate.py
          python psion/tests/validate.py
          python -m compileall -q cipher/tests psion/tests
          git diff --check
      - name: Commit patched tests
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add cipher/tests psion/tests
          git commit -m 'Follow active class schema in lifecycle tests'
          git push origin HEAD:agent/review-mod-completeness
